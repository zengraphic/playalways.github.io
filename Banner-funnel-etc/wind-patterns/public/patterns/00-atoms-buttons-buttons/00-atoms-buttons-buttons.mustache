<button class="btn btn-primary {{btnClass}}">{{ btnText }}</button>
<button class="btn btn-secondary {{btnClass}}">{{ btnText }}</button>
<button class="btn btn-ghost {{btnClass}}">{{ btnText }}</button>